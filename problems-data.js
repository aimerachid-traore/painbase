// Données statiques supprimées — le site affiche uniquement les données
// collectées par le pipeline (pipeline/collect.js → Supabase).
const PROBLEMS = [];

function getProblemById(id)       { return null; }
function getRelatedProblems()     { return []; }
